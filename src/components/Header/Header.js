import React, { Component } from 'react';
import './style.css';

export class Header extends Component {
  render() {
    return (
      <header>OpenCamera-Capstone</header>
    )
  }
}

export default Header;